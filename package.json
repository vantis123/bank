{
  "name": "bank",
  "version": "0.0.1",
  "description": "Bank by Arvantis — credit report intelligence for credit pros",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace=@bank/dashboard",
    "dashboard": "npm run start --workspace=@bank/dashboard",
    "mcp": "npm run start --workspace=@bank/mcp-server",
    "postinstall": "playwright install chromium",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "author": "Arvantis Tech",
  "license": "UNLICENSED",
  "devDependencies": {
    "handlebars": "^4.7.9",
    "playwright": "^1.59.1"
  }
}
